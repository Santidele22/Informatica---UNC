#include <iostream>

using namespace std;

int main(){
    string name = "santiago", city = "cordoba",state = "cordoba", postal_code = "5000";


    cout << "Mi nombre es " + name;
    cout << " vivo en " + city;
    cout << " en el estado de " + state;
    cout << " el codigo postal es " + postal_code;
    return 0;
}